<% flash.each do |key, value| %>
  <% if flash_bootstrap_name(key) %>
  <div class="alert alert-<%= flash_bootstrap_name(key) %>" role="alert">
    <%= value %>
  </div>
  <% else %>
  <div class="alert alert-<%= key %>" role="alert">
    <%= value %>
  </div>
  <% end %>
<% end %>
